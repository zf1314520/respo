eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
server:
  port: 8082
spring:
  application:
    name: service-feign
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
  rabbitmq:
    port: 5672
    host: localhost
    password: guest
    username: guest

feign:
  httpclient:
    enabled: true
  hystrix:
    enabled: true

